apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: myapp-dev
  namespace: flux-system
spec:
  interval: 1m0s
  url: https://github.com/Jackdstp/dstpflux.git
  ref:
    branch: dev
  secretRef:
    name: flux-system
